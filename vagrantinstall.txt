Screen saver off
Power saver off
Auto login on

sudo apt install vim ssh sshfs nfs-common net-tools 
sudo vim /etc/hosts
ssh-keygen 
ssh-copy-id juan@vbhost
mkdir -p ~/vbhost/data
mkdir -p ~/vbhost/home
sudo mount vbhost:/data ~/vbhost/data
sudo mount vbhost:/home/juan ~/vbhost/home

sudo apt install linux-headers-$(uname -r) build-essential dkms
Install VirtualBox Guest Addons
sudo sh /cdrom/VBoxLinuxAdditions.run



NAT MAC 08:00:27:35:ea:45
